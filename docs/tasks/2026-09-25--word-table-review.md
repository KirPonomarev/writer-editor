# WORD_TABLE_REVIEW_20260925

Base: 3a5f0b2080e861d779cf5838a2a34c6e7d5e4244. Owner authorizes solo Word-first implementation and full delivery. C1 tables are already delivered and eight exact-head IDs independently accepted; this slice targets the sixteen remaining C2/C3 table IDs without claiming complete Word portability.

## MAP → MOVE → PROVE

- Outcome: unchanged table geometry and cell ownership survive authorized tracked-text Apply, atomic save, fresh reopen and re-export. C3 executes five complete cycles.
- Truth and authority: saved scene doc-v2 and the authenticated main-owned export map; foreign XML and evidence projections cannot grant writer authority.
- Hypothesis: the parser currently treats every table occurrence as a structural change. Compare its bounded table projection against the authenticated export map before distinguishing unchanged table inventory from actual structural edits.
- Protected state: frozen 420 Word and 1120 overall IDs, all C1 receipts, mandatory hops, SOURCE/PACKAGED separation, existing Command Kernel and stale/recovery guards, unrelated WIP. No Google work or visual changes.
- Proof: focused positive and adversarial contracts, independent raw/native table reader, complete physical routes, mandatory repository/CI gates, merge and exact merged-head verification. Missing evidence grants zero cells.
- Rollback: revert this product PR and companion Lab code; retain all raw evidence and readable canonical project data.

## FEATURE_INTEGRATION_MANIFEST_V1

- featureId: WORD_TABLE_REVIEW; featureVersion: 1; integrationMode: EXISTING_SEAM.
- domainOwner: Product Core; authoritativeData: scene doc-v2 and authenticated active export map; derivedData: returned table projection and review candidates.
- commandIds: existing review intake, explicit Apply, save, export and reopen; queryIds: existing document, project and review projections; eventTypes: existing revision and lifecycle invalidation.
- productProjectionIds: activeDocument, projectTree, reviewSurface; capabilityIds: existing review/import/export capabilities.
- authorityMap: validated local source and returned structure -> immutable candidate -> Command Kernel revalidation -> existing atomic writer. No new writer, bus or provider controller.
- identityKeys: project, scene, source revision, generation, round, export, artifact hash, product/Lab HEAD and tree, runtime build/profile.
- revisionPolicy: check current source before preview publication and revalidate capability/current source at dispatch.
- writePath: existing authorized Apply and persistence ports; readPath: bounded namespace-aware parser and authenticated local export map.
- requiredProductPorts: existing document, parser, review, persistence and recovery ports; requiredDesignOsPorts: existing read-only review surface and intent dispatch.
- surfaceManifests: unchanged existing surfaces; slotRequirements: none; supportedWorkspaces: WRITE and REVIEW; adapterRequirements: existing Electron and native Word.
- stateClasses: PROJECT_STATE, AUTHORING_WORKING_STATE, DERIVED_STATE; persistenceClass: existing doc-v2; migrations: none.
- fallbacks: missing/forged source, changed topology, tracked table structure/property edits, stale identities and unsupported semantics remain blocked or explicitly manual.
- performanceBudget: one bounded topology comparison over already parsed paragraphs; no additional DOCX parse or typing-path work.
- accessibilityRequirements: preserve current table keyboard and visible cell behavior; securityBoundary: no new dependency, network, secret, filesystem or renderer authority.
- lifecycle: C2 full Word review/Apply/reopen/re-export; C3 five repetitions; recovery: existing atomic writes and readable rollback snapshots.

Declaration and clean-base preflight: WORD_TABLE_REVIEW_20260925. Default commit, push, PR, CI, merge and exact-head gates all remain required.
