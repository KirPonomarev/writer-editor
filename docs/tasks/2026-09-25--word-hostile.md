# Word hostile return proof

TASK_ID: WORD_HOSTILE_20260925
TYPE: CORE
CANON_VERSION: v1.0
CHECKS_BASELINE_VERSION: v1.0
BASE_SHA: 3dbe5404aad3b583fdbf929f2b49cf016f819fc6
DELIVERY_POLICY: COMMIT_REQUIRED=true PUSH_REQUIRED=true PR_REQUIRED=true MERGE_REQUIRED=true

## MICRO_GOAL

Prove typed rejection without canonical mutation for fourteen distinct malformed
Word return classes in C1, C2 and five-cycle C3, separately on SOURCE/PACKAGED.
The owner explicitly authorizes solo Word-first implementation and full delivery.
The eighty-four frozen hostile IDs remain candidates until independent official
verification on merged main. Valid supported fidelity failures never become
hostile passes. Google and mixed routes are deferred.

## ARTIFACT

One bounded product admission and companion registered Lab change: actual
command-path rejection observations, an independent bounded ZIP/XML classifier,
counterexamples, and integration into the existing official aggregate.

## ALLOWLIST

Exact paths are the twenty-four paths in the preflight-passed declaration
WORD_HOSTILE_20260925. Product implementation uses the existing parser, command
boundary, official manuscript consumer and certification seams. Companion Lab
changes are isolated in codex/interop-word-hostile-lab-20260925. The active table
Lab and its evidence writer are protected. No new provider may run and main may
not advance before its physical batch and official acceptance finish.

## DENYLIST

Unrelated worktrees, private documents, new dependencies, product network, UI
geometry, new command buses, denominator changes, shortened C3, weakened
oracles, fabricated classifications, direct main pushes, reset, stash, clean,
force push and undeclared base movement.

## CONTRACT / SHAPES

FEATURE_INTEGRATION_MANIFEST_V1:

- featureId: WORD_HOSTILE; featureVersion: 1; integrationMode: EXISTING_SEAM.
- domainOwner: Product Core; authoritativeData: saved project/scene/notes and
  authenticated local export map; derivedData: rejected-input diagnostics.
- commandIds: existing Review intake and explicit Apply; queryIds: current
  project, document and review projections; eventTypes: existing revision and
  lifecycle invalidation; capabilityIds: existing review/import/export policy.
- productProjectionIds: activeDocument, projectTree, reviewSurface;
  authorityMap: untrusted input -> bounded parser -> immutable diagnostic ->
  kernel revalidation -> typed refusal; no new mutation authority.
- identityKeys: project, scene, revision, generation, round, artifact, native
  process, product/Lab commit and runtime profile; revisionPolicy: recheck at
  dispatch and publication.
- writePath: existing kernel and atomic writer only; readPath: existing parser
  and independent evidence reader; requiredProductPorts: import, review,
  persistence and recovery; requiredDesignOsPorts: existing projection/intent.
- adapterRequirements: existing Electron and Word; surfaceManifests and
  slotRequirements: existing surfaces unchanged; supportedWorkspaces: WRITE,
  REVIEW; platformAvailability: current pinned desktop Word profile.
- stateClasses: PROJECT_STATE, AUTHORING_WORKING_STATE, DERIVED_STATE;
  persistenceClass: unchanged; migrations: none; accessibilityRequirements:
  preserve existing typed diagnostic and keyboard paths.
- fallbacks: malformed or ambiguous inputs fail closed; valid unsupported data
  is never independently labelled invalid merely because the product refuses it.
- recovery: canonical bytes and authoring buffer survive each refusal, followed
  by successful valid control and fresh-process reopen; rollback: revert this PR
  and companion Lab source, retain all evidence.
- performanceBudget: fourteen bounded probes share each physical route;
  securityBoundary: disposable synthetic bytes, no escape writes or network;
  lifecycle: one native controller, C3 five complete physical rounds.
- negativeBypassChecks: forced Apply, stale-session reuse, altered snapshot,
  wrong field, valid bytes, omitted round/hop, forged head/profile/classification.
- evidenceBindings: exact merged SHA, admitted Lab source hashes and every raw
  artifact; currentReality: generic hostile schema exists, physical acceptance
  of these eighty-four IDs does not.

MAP: field-specific invalidity is established independently from exact returned
bytes; command rejection is a different observation. Native Word supplies the
valid carrier before bounded corruption. Each rejection interval retains the
full project inventory; canonical files are checked byte-for-byte while named
derived diagnostic artifacts remain separately accounted. Valid controls and
all mandatory hops are retained. No control run awards duplicate fidelity IDs.

## IMPLEMENTATION_STEPS

1. CHECK_01 runs before changes; CHECK_02 onward run after implementation.
2. Add failing classifier and admission counterexamples, then the smallest
   coherent implementation using existing commands and the manuscript driver.
3. Execute one bounded candidate pilot, fix observed product failures, and
   complete required tests and delivery before merged physical acceptance.
4. Run six Word route/profile batches and the single official aggregate.

## CHECKS

CHECK_01_PRE_IDENTITY: bootstrap, ordered startup reads, registered writable
encrypted T7 identity, clean exact base, declaration preflight and task gate.
CHECK_02_POST_FOCUSED: independent positive/negative classifier tests, command
refusal tests and evidence tamper controls; preserve prior manuscript proof.
CHECK_03_POST_DELIVERY: affected chain, mandatory baseline/certification/CI,
diff review, commit/push/PR/merge, clean exact merged-head revalidation.
CHECK_04_POST_PHYSICAL: actual Word hops, separate profiles, five C3 cycles,
zero canonical mutation on every hostile interval and official accepted IDs.

## STOP_CONDITION

Stop the affected experiment on ambiguous identity, foreign state, missing
mandatory evidence or the third identical failure. Repair the cause within the
owner-authorized scope; do not weaken the gate or relabel the failure PASS.

## REPORT_FORMAT

AGENT_FINAL_REPORT_V1 and CODEX_OUTPUT_POLICY. Report exact before/after/merged
SHA, changed basenames, executed checks, delivery and residual limitations.
Count only unique official accepted IDs, with recovery separate from firsts.

## FAIL_PROTOCOL

Retain original failure bytes, expected/actual, exact head and hashes. Test one
different hypothesis cheaply before another complete run. Do not restart a
successful baseline without changed inputs. Any incomplete proof has zero credit.
