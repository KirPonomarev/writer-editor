# C4 Office-mode DOCX transport: section-boundary prerequisite

The frozen C4 route is an Office-mode DOCX round trip. A physical Google Office run on product `fcf75a4` retained suggestions and comments but removed an authored empty paragraph that carried a section break. The returned document changed from 35 to 34 paragraphs and moved the first section boundary from index 26 to 25, so product return intake correctly rejected it with `RTK_RETURN_INTAKE_DOCUMENT_SECTIONS_MISMATCH`. Google also omitted the explicit zero gutter and default single-column XML; those omissions did not change the effective section layout.

This change adds an opt-in export encoding for that empty boundary paragraph using U+2060 WORD JOINER. A separate physical diagnostic DOCX retained all 35 paragraphs and the section boundary after a Google Office save and reload. Normal Word exports do not use the marker. Return validation permits only the two observed default-property omissions and records them; it still checks the signed source digest and rejects changed page geometry or section topology. The product project remains protected by the existing full-manuscript command, authority and return-intake gates.

This is a zero-cell runtime prerequisite. The diagnostic DOCX and earlier failed Lab run are not acceptance evidence. New C4 credit requires a fresh physical round trip on the merged runtime, independent raw readback, product intake/apply/reopen, C4 policy/verifier admission and official aggregate verification on the exact merged SHA. If the marker appears in canonical scene text or any other protected field changes, the run must fail closed.
