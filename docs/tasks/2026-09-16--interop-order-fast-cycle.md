# Repeatable ORDER C1 full acceptance cycle
TYPE: OPS_WRITE
CANON_VERSION: v3.13a-final
CHECKS_BASELINE_VERSION: v1.0
TASK_ID: INTEROP_ORDER_C1_FAST_CYCLE_20260916
DELIVERY_POLICY: COMMIT_REQUIRED=true PUSH_REQUIRED=true PR_REQUIRED=true MERGE_REQUIRED=true
STATUS: IMPLEMENTATION
DOCUMENT_CLASS: PROCESS
CLAIM_BOUNDARY: ONE_KNOWN_ORDER_RECIPE_NO_BROAD_PORTABILITY_CLAIM

## MICRO_GOAL
Execute the existing synthetic ORDER__SINGLE_SCENE__C1__SOURCE_RUNTIME recipe,
independently verify every stage, admit it through verifyInterop100 and repeat
without changing checker source or manually authoring success receipts. Measure
the complete request including preparation, provider work, checks, admission and
durable report. The 300-second repeat target is not an achieved benchmark.

## ARTIFACT
One bounded repository-owned raw checker and recipe policy, the existing
denominator CLI integration, local Lab acceptance integration and full timing.
The owner explicitly authorized implementation of final plan V2 personally,
without agents, on 2026-09-16. This is direct OPS_GOVERNANCE work outside the R2.4
StageAdmission V2 program transition; no stage lease is manufactured.

The new MACHINE_RECIPE_REVIEW mode is limited to this known fixture and route.
It executes an independent repository-owned raw reader with the previously
physically reviewed literal expectation, positive equivalence controls and
semantic mutants. It publishes an actual read-only evidence index. This mode
does not impersonate EXTERNAL_SUPERVISOR or claim a Design Lab review. The classic
external supervisor and preview path remains required for all other Lab cells.
Evidence bytes never authorize executing producer-supplied code or selecting an
expected answer. New fixtures, routes, providers and outcome policies need their
own reviewed implementation. No product runtime or preservation standard changes.

## ALLOWLIST
- docs/tasks/2026-09-16--interop-order-fast-cycle.md
- docs/OPS/RTK/YALKEN_INTEROP_ORDER_C1_RECIPE_POLICY_V1.json
- scripts/ops/rtk-interop-order-c1.mjs
- scripts/ops/rtk-interop-order-c1-readback.py
- scripts/ops/rtk-interop-100-denominator-v1.mjs
- test/contracts/rtk-interop-100-denominator.contract.test.js
- scripts/ops/r24/corrective/post-audit-certification-set.mjs
- docs/OPS/R24/CORRECTIVE/C1B_TEST_INVENTORY_V1.json
- docs/OPS/RTK/YALKEN_INTEROP_100_GOVERNANCE_CHANGE_APPROVALS_V1.json
- docs/OPS/R24/CORRECTIVE/PK1R1_GOVERNANCE_CHANGE_APPROVALS_V1.json
- docs/OPS/R24/CORRECTIVE/C2A_GOVERNANCE_CHANGE_APPROVALS_V1.json

Local Lab companion scope: cli/lab.mjs, scripts/run_sequential.py,
src/order-machine-review.mjs, test/order-machine-review.test.mjs, README.md and
data/rtm/rtm-v2.json and LAB_MANIFEST.json (merged runtime identity only).
Lab code/evidence receives local commits only; its existing
remote publication prohibition remains. Changes to generated ledger, artifact
index and dashboard occur only through their existing owned runtime writers.

## DENYLIST
- src
- package.json
- package-lock.json
- archived TEXT successor and its two raw verifier files
- frozen product denominator, historical ledger/envelope and old run artifacts
- owner projects, private documents, cloud/network runtime, timers and agents
- new dependencies, broad field/route support or synthetic physical credit

## CONTRACT / SHAPES
FEATURE_INTEGRATION_MANIFEST_V1:
- featureId/version: INTEROP_ORDER_C1_RECIPE / 1; integrationMode: EXISTING_SEAM.
- domainOwner/authoritativeData: existing product denominator and current actual
  runtime; no manuscript truth is moved into Lab.
- derivedData: independent observations, receipt, evidence index and timings.
- commandIds/eventTypes/queryIds: existing verifyInterop100 OPS query; actual
  runtime uses cmd.project.docx.previewContent, previewImportPlan and
  importSafeCreate, unchanged; no new product command or event.
- productProjectionIds/capabilityIds: no new runtime projection or capability.
- authorityMap: owner task authorizes implementation; trusted checker decides
  bounded admission; a producer PASS, hash or dashboard never grants authority.
- identityKeys: contract, recipe, product HEAD/tree, Lab source identity, exact
  RUN/observation, complete artifact inventory, provider build, fixture digest.
- revisionPolicy: recheck identities and immutable bytes immediately before
  publication; stale/wrong identity fails.
- writePath/readPath: existing Lab ledger and report writers; product consumer
  reads raw snapshots and computes one unique bounded cell decision.
- requiredProductPorts/adapterRequirements: existing runtime Word and import
  adapters; bounded evidence filesystem and repository-owned Python subprocess.
- requiredDesignOsPorts/surfaceManifests/slotRequirements: no product UI change;
  diagnostic evidence index only, no new Design OS surface.
- supportedWorkspaces/platformAvailability: existing desktop source runtime,
  exact qualified Word build; no packaged or Google claim.
- accessibilityRequirements: no product UI contract change; report is readable.
- fallbacks: explicit failure/unknown, zero admission credit.
- stateClasses/persistenceClass: derived evidence, atomic local publication;
  project and authoring state remain in unchanged product runtime.
- migrations/recovery/rollback: no project migration; retry exact request without
  double effect/credit; recover report from committed decision; one PR revert.
- performanceBudget: complete repeat goal 300 seconds; no hot-path changes.
- securityBoundary: bounded no-follow regular files, no external executable,
  input validation before interpretation, isolated disposable synthetic project.
- lifecycle: preflight, execute, seal, independent check, controls, Lab decision,
  product decision, required publication, report.
- negativeBypassChecks: coherent content tamper, missing artifacts, wrong RUN,
  stale head, wrong route/provider, unknown XML, double credit and crash recovery.
- evidenceBindings/currentReality: old TEXT capsule stays immutable; known ORDER
  driver exists; new recipe admission remains unclaimed until executed/delivered.

MAP: fixed product target -> approved recipe -> exact native observation -> raw
reader -> explicit machine Lab gate -> official product decision -> durable report.
MOVE: one coherent product/Lab vertical slice, not a new registry or service.
PROVE: per-run native lifecycle and independent bytes; no count-only PASS.

## IMPLEMENTATION_STEPS
1. Confirm clean a453fdf2 base and preflight; preserve the existing runtime shadow.
2. Implement constant raw ORDER checks, positive controls and semantic mutants.
3. Add exact RUN machine recipe gate and bounded official consumer integration.
4. Preserve classic review policy and immutable historic evidence.
5. Test failure/retry/publication behavior and refresh required governance bindings.
6. Complete product PR delivery, bind the Lab to the merged runtime, execute one
   fresh case, read its official result, repeat and measure the complete cycle.

## CHECKS
CHECK_01_PRE_IDENTITY: clean exact base, T7 identity, bootstrap and architecture preflight.
CHECK_02_POST_FOCUSED: focused ORDER raw/gate tests plus existing denominator regression.
CHECK_03_POST_LAB: Lab exact-run, retry, changed-file, cleanup and timing tests.
CHECK_04_POST_PHYSICAL: actual Word and fresh-process import/reopen; all six ORDER
subcases, required oracle classes, controls, raw hashes and official decision.
CHECK_05_POST_DELIVERY: current inventory, approvals/change detection, ops-gate, guardrails,
OSS policy and required CI; clean merged identity and actual post-merge evidence.

## STOP_CONDITION
No acceptance on missing independent raw proof, authority/scope drift, changed
source or artifact, dirty unrelated state, failed mandatory check or wrong route.
Three identical failures stop that experiment with expected/actual and one next
hypothesis. No new contour begins before this delivery chain closes.

## REPORT_FORMAT
One text block: task, before/after/merged SHA, changed basenames, tests, commit,
push, PR, CI, merge, exact-head proof, full timings, unique credit and next step.

## FAIL_PROTOCOL
Keep raw evidence and timing of failed attempts. Pending acceptance/publication
stays pending; historical and diagnostic proof never become current credit.

### Serialization compatibility repair within the same contour
The first merged physical run passed native checks but product admission rejected
ORDER_OBSERVATION_HASH. Lab v2 sorts observation keys with localeCompare; the new
review index uses ordinal keys. Revision 2 reproduces the qualified en-US producer
serialization for that existing hash and preserves ordinal independent indexes.
Raw evidence and the failed attempt remain immutable and uncredited.

A zero-credit artifact replay now executes the complete raw path before delivery,
while the official admission wrapper retains clean-current-main, Lab source,
provider, freshness, invalidation and exact identity gates. Historical v1 policy
bytes remain recognizable by the historical governance verifier only. The first
delivery of each explicitly reviewed policy digest freezes its implementation;
ordinary later drift under the same digest remains rejected. No new field, fixture,
route, product command or runtime change is introduced by this correction.
