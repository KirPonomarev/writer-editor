# W5 — bounded Word table properties

Task: WORD_TABLE_PROPERTIES_20260925. Base: e53782a9d1fdbae35dcb11d8df5d5f8ca54a0eeb.
Owner authority: implement the Word remediation plan, both directions, without weakening proof or acceptance.

O: The 720/4320 dxa grid, literal red fill and double 24 border survive import, ordinary editing, save/reopen and native Word re-export in SOURCE and PACKAGED.
T: Validated OOXML -> pure canonical document properties -> existing immutable editor projection / existing governed save -> DOCX serialization. No new writer, storage port or authority.
H: Table metadata currently retains geometry only; a bounded property model carried with the same ownership will eliminate the demonstrated replacements without changing topology or text authority.
B: Preserve cell ownership, spans, identities, native routes, limits, atomic persistence/recovery, old evidence, denominator and unknown-input diagnostics. No Google, UI controls, dependencies or network runtime. Revert this slice for rollback; no migration or mass rewrite.
P: Raw counterexamples and hostile values, five cycles, both merge axes, legacy tables, actual editor edit/undo/redo, independent raw reader, actual native Word, required baseline/catalog/governance/CI and exact merged verification. Local prerequisite work yields zero accepted IDs.
I: Exact task base above; physical proof binds final commit, profile, runtime binary and artifact hashes.

## FEATURE_INTEGRATION_MANIFEST_V1

Product plane: optional bounded table/cell properties in canonical rich document data, validated before interpretation. Interface plane: mechanical rendering of those properties in existing semantic table nodes. No new surface; SURFACE_MANIFEST_V1 not applicable.
Commands: existing generic import safe-create, authoring save, full manuscript export and authenticated review intake. Queries: existing immutable document and review projections. Events: existing save/import events. Effects: existing fenced filesystem and DOCX adapters. Product ports, lifecycle/project/revision guards, capability revalidation and recovery remain unchanged.
Design OS read-only port: existing Tiptap document projection. No document truth in shell state. No renderer platform/storage access.

## Bounded property profile

Optional versioned properties preserve positive integer absolute grid widths (1–31680 dxa per column; bounded total), absent width, fixed layout, absolute preferred table width, literal RGB/none shading, and literal none/single/double borders (2–96 eighth-points, RGB or auto). Cell widths matching the grid are redundant; conflicts and relative widths require typed diagnostics. Border edges retain table defaults and explicit cell overrides. Namespace, property owner and duplicate checks precede use. Theme, pattern, automatic layout and complex inheritance are not guessed.
Legacy tables retain their historical default representation. New explicit absent widths do not become 1440.

## Interface discovery

Existing Vanilla CSS and Tiptap/ProseMirror, existing styles.css tokens and table keyboard behavior. ui-craft applied to mechanical content presentation. The author document's colors/widths are data, not changes to the product palette or layout. No design composition decisions; DESIGN_TOOL_ROUTER=NOT_APPLICABLE. No new project design memory, toolbar or global styles.

## Claim

TARGET until required tests, native proofs and delivery finish. W7 remains responsible for the unified official accepted set; this task does not self-award cells.
