# W7 native Word auto-fit table review repair

TASK_ID: WORD420_TABLE_AUTOFIT_CLOSURE_20260925
BASE_SHA: 0bbbdcdc8aeec1c7d598cd93e99cc281eb0c2e57
SCOPE: Owner-approved Word remediation, final acceptance blocker.

## MAP → MOVE → PROVE

O: A real tracked Word return of a legacy auto-fit table opens a read-only review preview and permits only the existing authenticated explicit Apply. Explicit table properties remain protected.
T: Bounded native DOCX parse -> immutable table projection -> authenticated local export map -> existing review command revalidation -> existing fenced Apply and recovery. No authority comes from returned table metadata.
H: Word recomputed legacy grid hints from 1440 dxa to 5772/1749/1495 during tracked editing. W5's strict property comparison mistook the derived auto-fit grid for changed stored geometry. Compare this one derived dimension only when the local source has no explicit wordTable properties; compare every other property exactly.
B: Preserve all original source/returned bytes, failed evidence, 64 completed physical runs, frozen 420 Word IDs, five C3 rounds, both profiles, no-write preview, explicit geometry, topology, cell properties, limits and unrelated work. No Google, dependencies, UI changes or new persistence owner. Rollback is the bounded W7 change.
P: Two targeted regressions fail on the previous code. Both full contract files pass after repair. The retained native returned DOCX parses and binds against its original local export map with automaticApplyAuthority=false. Fresh SOURCE/PACKAGED native canaries, all affected contracts, mandatory baseline/security/CI and final exact merged official aggregate remain required. Prior runtime evidence is historical and cannot be promoted across this product change by widening an allowlist.
I: Failure run ORDER__SINGLE_SCENE__C2__SOURCE_RUNTIME__tables-v1-2026-09-25T20-47-12-642Z on runtime 10a21a9394f13e058cac248e2d4f4c0f86a9263f. Failure: RTK_RETURN_INTAKE_TABLE_TOPOLOGY_MISMATCH. Before/after manifest bytes match; the owned runtime process exited. Native source and result hashes remain in the retained Lab evidence.

## FEATURE_INTEGRATION_MANIFEST_V1

Product plane: existing pure canonical table comparison. Interface plane: unchanged immutable review projection. Commands: existing preview and explicit authenticated review Apply. Queries: existing preview. Events/effects/ports: unchanged existing review outcome, DOCX parsing and fenced filesystem adapters. State classes: derived immutable comparison and protected canonical project state. Identity and capability revalidation are unchanged. No new visual surface; DESIGN_TOOL_ROUTER=NOT_APPLICABLE and SURFACE_MANIFEST_V1 is not applicable. Recovery uses the existing fenced journal. Negative tests retain rejection of explicit grids, fixed layout, total width, shading, borders, cell changes, invalid widths and moved tables. Raw evidence is never normalized or mutated.

## Claim boundary

Only a validated positive bounded grid may vary for the legacy implicit auto-fit source. Explicitly stored properties, including an explicit default-valued grid, remain exact. The comparison yields no write capability. This is a product repair and awards zero cells until fresh official acceptance; it is not a universal Word layout-equivalence claim.
