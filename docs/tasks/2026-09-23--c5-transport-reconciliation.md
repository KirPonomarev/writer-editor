# C5 transport admission reconciliation

STATUS: CANDIDATE
DOCUMENT_CLASS: PROCESS
CLAIM_BOUNDARY: Scoped verifier contract for C5 manuscript evidence; no new physical cell credit or change to the frozen 1120-cell axes.
TASK_ID: C5_TRANSPORT_RECONCILIATION_20260923
BINDING_BASE_SHA: ab8385ced8b07ffd2f797d2cfd7000061491c48c
AUTHORITY: Owner-requested correction, regression test, and direct briefing to the active implementation task.
DESIGN_TOOL_ROUTER: NOT_APPLICABLE

The frozen denominator V1 records a historical Google connector qualification that required an internal uploaded-file reference and rejected direct local-path import. The current C5 manuscript policy has a later, narrowly scoped direct native-conversion qualification. Its route qualification alone still earns no cell. Only a complete current-head native readback, DOCX return, durable Yalken proof, and independent field oracle may admit a C5 cell.

The batch verifier must bind the current C5 policy to the exact frozen V1 bytes, check that the archived transport assumptions remain unchanged, and expose the selected evidence-mode resolution in its report. A cross-mode contract test must show that the generic V1 validator still rejects direct-path qualification while the current manuscript C5 resolver accepts only the scoped policy. Mutated scope, historical steps, direct-path support, or qualification credit must fail closed.

No frozen denominator edit, product runtime change, provider action, new dependency, or historical receipt rewrite is in scope. The `ab8385` 278-cell physical result remains evidence for that exact head only; this task does not inherit it as proof for a later merged head. Rollback reverts this one verifier and companion binding change while preserving all immutable evidence.
