# Word media preservation

TASK_ID: WORD_MEDIA_20260925
TYPE: CORE
CANON_VERSION: v1.0
CHECKS_BASELINE_VERSION: v1.0
BASE_SHA: 1c9dae7a3dcdc3fc79e22ea9b53b0c2ca67bcd5d
DELIVERY_POLICY: COMMIT_REQUIRED=true PUSH_REQUIRED=true PR_REQUIRED=true MERGE_REQUIRED=true

## MICRO_GOAL

Preserve owned PNG image bytes, stable identity, placement and alt text through
Yalken export, native Word lifecycle, typed intake/apply and reopening. All 24
frozen Word MEDIA_ASSETS combinations remain candidates until independent
physical proof and exact merged-head official admission. Existing 84 hostile
IDs on the base are a separate accepted packet. No historical count is carried
forward without the official verifier. Owner authorizes solo Word-first work.

## ARTIFACT

One coherent media path with canonical data, existing atomic persistence,
existing editor content surface, DOCX package reader/writers, independent
readback and qualified physical recipe. No new dependency or runtime network.

## ALLOWLIST

Exact 44 paths in the preflight-passed WORD_MEDIA_20260925 declaration. Companion
Lab must use a separate owned registered worktree and exact product admission.
Current hostile evidence Lab and its shadow are retained without mutation.

## DENYLIST

Other worktrees, owner documents, dependencies, shell redesign, new command bus,
remote resource loads, arbitrary paths from DOCX/renderer, evidence substitution,
denominator/hop/cycle reductions, manual sum across heads and forced Git moves.

## CONTRACT / SHAPES

FEATURE_INTEGRATION_MANIFEST_V1:

- featureId: WORD_MEDIA; featureVersion: 1; integrationMode: EXISTING_SEAM.
- domainOwner: Product Core; authoritativeData: saved document and owned media;
  derivedData: renderer image source and import diagnostics.
- commandIds: existing export, typed import preview/create, review intake/apply,
  document save; queryIds: activeDocument, projectTree, reviewSurface;
  eventTypes: existing document revision and lifecycle events;
  capabilityIds: existing kernel export/import/review/save capabilities.
- authorityMap: untrusted bytes -> bounded PNG/ZIP/XML validation -> main-owned
  immutable preview -> kernel and source revision revalidation -> atomic write.
- productProjectionIds: existing activeDocument and reviewSurface; identityKeys:
  project, scene, source revision, generation, asset hash, run and native target.
- writePath: existing Command Kernel and atomic file port; readPath: owned
  project data and bounded package parser; requiredProductPorts: existing
  persistence, import/export and review adapters; requiredDesignOsPorts:
  existing read-only document projections and intent dispatch.
- adapterRequirements: Electron and native Word; surfaceManifests: existing
  editor content only; slotRequirements: none; supportedWorkspaces: WRITE,
  REVIEW; platformAvailability: pinned current Mac Word SOURCE/PACKAGED.
- stateClasses: PROJECT_STATE, AUTHORING_WORKING_STATE, DERIVED_STATE;
  persistenceClass: readable document and owned media files; migrations: no
  destructive migration; old text-only documents preserve behavior.
- accessibilityRequirements: native image alt text and existing keyboard
  selection; no new animation, toolbar or shell composition.
- fallbacks: fail closed on malformed or unsupported input, identity mismatch
  or stale result; never silently flatten valid media. No URI creates a path.
- recovery: preserve authoring text; mixed scene and asset write uses the same
  journal/rollback boundary; retained originals and raw return remain readable.
- performanceBudget: explicit resource bounds and asset reuse; no binary I/O
  in renderer; securityBoundary: owned synthetic local files, no remote images.
- lifecycle: one native controller and exact project/build/profile identity;
  negativeBypassChecks: unsafe relationship, substituted bytes, dropped image,
  changed alt/placement, invalid PNG, stale projection and partial activation.
- evidenceBindings: all artifact hashes, raw independent readback and exact
  merged SHA; currentReality: product media implementation and native diagnostic probes exist;
  official MEDIA acceptance remains zero until complete delivery and proof.

O: observe equivalent media bytes and semantics after the complete Word route.
T: canonical project -> existing kernel capability -> existing atomic adapter.
H: typed binary batch entries prevent binary truncation while preserving text
and rollback; the same validated media graph must drive transport and editor.
B: preserve old scene bytes, foreign WIP, all existing gates and denominator;
rollback the one PR, retaining assets and evidence.
P: first mixed text/binary success and injected rollback, then per-layer
negative controls, one actual native pilot, mandatory full delivery and proof.
I: exact base above; synthetic owned documents; admitted Lab source hashes,
Node 22.12.0/npm 10.9.0 and independently verified SOURCE/PACKAGED builds.

UI reference: Lazyweb Agentic Search 9b5d6e28-ed1e-4bd7-b451-e8223dd781fa,
inline image content pattern; existing Yalken tokens and composition retained.
No external reference creates product authority.

## IMPLEMENTATION_STEPS

1. CHECK_01 before write: bootstrap, unchanged ordered canon, T7 identity,
   architecture declaration/preflight and exact affected source reads.
2. Failing binary persistence regression; preserve exact bytes and rollback.
3. Bounded media model and transport; typed canonical import/apply and editor.
4. Independent counterexamples and smallest native pilot, then whole packet.
5. Required tests, CI, commit/push/PR/merge and exact merged official acceptance.

## CHECKS

CHECK_01_PRE_IDENTITY: exact clean base, canon, owner scope and preflight.
CHECK_02_POST_FOCUSED: raw bytes, field identity, atomic rollback and stale guards.
CHECK_03_POST_DELIVERY: affected chain, baseline, inventory, security and required
CI without skips or self-PASS; review complete diff and full delivery chain.
CHECK_04_POST_PHYSICAL: complete C1/C2/C3 five cycles, separate profiles and full
volume fixtures; current unique accepted IDs only from official aggregate.

## STOP_CONDITION

Repair exact observed failures within authorized scope. After third identical
failure retain evidence and use another hypothesis. Never weaken the oracle.

## REPORT_FORMAT

AGENT_FINAL_REPORT_V1 and CODEX_OUTPUT_POLICY; exact identities, changed basenames,
checks and delivery, observed numerator, remaining limitations and next step.

## FAIL_PROTOCOL

Retain expected/actual raw bytes, hashes and original failure; no silent retry,
manual evidence promotion or acceptance claim from a successful unit test.

Development checkpoints 239db73a and 594e3789 are not merged acceptance. Native
Word tracked rewrite preserved PNG bytes and three placements; native text uses
slash object markers, so field acceptance additionally requires Word inline-shape
object readback. The XML parser remains platform-neutral: its bounded package
adapter validates PNG bytes and supplies immutable metadata through a read-only
port. Adjacent text Apply preserves images; crossing an image stays blocked.
